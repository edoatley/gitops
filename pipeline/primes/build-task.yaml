apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: build-task
  namespace: primes
spec:
  workspaces:
  - name: source
  steps:
    - image: registry.redhat.io/ubi7/ubi-minimal
      script: |
        #!/usr/bin/env bash
        cd source
        ./gradlew clean build test